{
  "name": "biologi-1",
  "private": true,
  "version": "1.0.0",
  "description": "Studieside for Biologi 1 (Bios 1) - sammendrag, begreper og quiz",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "verify": "node build.mjs && node scripts/verify.mjs",
    "serve": "node scripts/serve.mjs",
    "dev": "node build.mjs && node scripts/serve.mjs"
  },
  "engines": { "node": ">=18" }
}
